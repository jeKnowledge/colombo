<h1>Reservations list</h1>

<div class="table-responsive">
  <table class="table">
  <thead>
    <tr>
      <th>Company</th>
      <th>Address</th>
      <th>Products</th>
      <th>Planned audit date</th>
      <th>Summary</th>
      <th>Price</th>
      <th>Proposed fee</th>
    </tr>
  </thead>

  <tbody>
    <% @reservations.each do |reservation| %>
      <tr>
        <td><%= reservetion.plan.auditor.company %>
        <td><%= reservetion.plan.auditor.address %></td>
        <td><%= reservetion.plan.roducts %></td>
        <td><%= reservetion.plan.price %></td>
        <td><%= reservetion.plan.date %></td>
        <td><%= reservetion.plan.summary %></td>
        <td><%= reservetion.plan.fee %></td>
        <td><%= reservetion.plan.validated %></td>
      </tr>
    <% end %>
  </tbody>
</table>
</div>
